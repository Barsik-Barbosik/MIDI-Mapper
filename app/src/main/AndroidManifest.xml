<?xml version="1.0" encoding="utf-8"?>
<manifest package="barsikbarbosik.midimapper" xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-sdk android:minSdkVersion="23" android:targetSdkVersion="33" />

    <!-- Optional: declare MIDI feature (not required on all devices) -->
    <uses-feature android:name="android.software.midi" android:required="false" />

    <!-- if connecting actual USB devices using USB host mode -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>